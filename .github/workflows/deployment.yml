name : Deploy to EC2

on: 
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: executing connect to server using ssh key
        uses: appleboy/ssh-action@master
        with:
          # host: ${{ secrets.HOST }}
          # username: ${{ secrets.USERNAME }}
          # key: ${{ secrets.KEY }}
          # port: ${{ secrets.PORT }}
           script: |
            cd /home/ubuntu/kutsuya
            git pull origin main
            docker stop kutsuya
            docker rm kutsuya
            docker build -t kutsuya-images:latest .
            docker run -d -p 80:8000 -e SERVER_PORT="8000" -e DB_USERNAME="admin" -e DB_PASSWORD="qwerty123" -e DB_HOST="database-2.c3aa78kamft1.us-west-2.rds.amazonaws.com" -e DB_PORT="3306" -e DB_NAME="db_kutsuya" --name kutsuya kutsuya-images:latest

          #   docker run -d -p 80:8000 -e SERVER_PORT=${{ secrets.SERVER_PORT }} -e DB_USERNAME=${{ secrets.DB_USERNAME }} -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} -e DB_HOST=${{ secrets.DB_HOST }} -e DB_PORT=${{ secrets.DB_PORT }} -e DB_NAME=${{ secrets.DB_NAME }} --name kutsuya kutsuya-images:latest
             

       